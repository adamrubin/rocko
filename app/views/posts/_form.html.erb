<%= semantic_form_for @post, :url => posts_url, :html => {:onSubmit => 'return sendMessage();'} do |form| %>
	<%= form.inputs do %>
		<%= form.input :message, :placeholder => "e.g., hello world"%>
	<% end %>
	<%#= form.buttons do %>
		<%#= form.commit_button :button_html => { :class => "btn big" } %>
	<%# end %>	
<% end %>