<%= semantic_form_for @post, :remote => true do |form| %>
	<%= form.inputs do %>
		<%= form.input :message, :placeholder => "e.g., hello world"%>
	<% end %>
	<%= form.buttons do %>
		<%= form.commit_button :button_html => { :class => "btn big", :disable_with => 'Wait...' } %>
	<% end %>	
<% end %>